<div class="max-w-4xl mx-auto">
  <%= admin_breadcrumbs(@breadcrumbs) %>

  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">Edit Announcement</h1>
    <p class="text-gray-600 mt-2"><%= @announcement.title %></p>
  </div>

  <%= render "form" %>
</div>
