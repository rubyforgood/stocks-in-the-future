<%# There is no separate edit page in this shape: the record's own page edits in place. This template stays so
    every existing Edit link keeps working, and renders that page. %>
<%= render template: "admin/portfolio_transactions/show" %>
