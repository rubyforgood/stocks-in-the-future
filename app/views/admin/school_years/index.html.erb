<div class="max-w-7xl mx-auto">
  <%= admin_breadcrumbs(@breadcrumbs) %>

  <div class="bg-white shadow-sm ring-1 ring-slate-900/5 rounded-lg overflow-hidden">
    <div class="px-4 py-5 lg:px-6 border-b border-slate-200 flex justify-between items-center">
      <h3 class="text-lg font-semibold text-slate-900">School years</h3>
      <div class="flex gap-2">
        <%= link_to "New school year", new_admin_school_year_path,
                    class: "inline-flex items-center px-4 py-2 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-sitf-primary hover:bg-sitf-primary-dark" %>
      </div>
    </div>

    <div class="overflow-x-auto">
      <table class="min-w-full divide-y divide-slate-300">
        <thead class="bg-slate-50">
          <tr>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">
              <%= sort_link(:id, "ID") %>
            </th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">
              School
            </th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">
              Year
            </th>
            <th scope="col" class="px-3 py-3.5 text-left text-sm font-semibold text-slate-900">
              <%= sort_link(:created_at, "Created At") %>
            </th>
            <th scope="col" class="relative py-3.5 pl-3 pr-4 lg:pr-6">
              <span class="sr-only">Actions</span>
            </th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-200 bg-white">
          <% if @school_years.any? %>
            <% @school_years.each do |school_year| %>
              <tr class="hover:bg-slate-50 transition-colors cursor-pointer"
                  data-controller="clickable-row"
                  data-clickable-row-url-value="<%= admin_school_year_path(school_year) %>"
                  data-action="click->clickable-row#navigate">
                <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-900">
                  <%= school_year.id %>
                </td>
                <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-900">
                  <%= school_year.school_name %>
                </td>
                <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-900">
                  <%= school_year.year_name %>
                </td>
                <td class="whitespace-nowrap px-3 py-4 text-sm text-slate-900">
                  <%= school_year.created_at.strftime("%B %d, %Y") %>
                </td>
                <td class="relative whitespace-nowrap py-4 pl-3 pr-4 text-right text-sm font-medium lg:pr-6">
                  <div class="flex justify-end gap-2">
                    <%= link_to "View", admin_school_year_path(school_year), class: "text-blue-600 hover:text-blue-800" %>
                    <%= link_to "Edit", edit_admin_school_year_path(school_year), class: "text-slate-600 hover:text-slate-800" %>
                    <%= link_to "Delete", admin_school_year_path(school_year),
                                data: { turbo_method: :delete, turbo_confirm: "Are you sure?" },
                                class: "text-red-600 hover:text-red-800" %>
                  </div>
                </td>
              </tr>
            <% end %>
          <% else %>
            <%= render "admin/shared/empty_row",
                       colspan: 5,
                       title: "No school years yet",
                       body: "A school year holds the quarters that earnings are calculated against.",
                       action_label: "New school year",
                       action_path: new_admin_school_year_path %>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
