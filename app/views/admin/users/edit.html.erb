<div class="max-w-7xl mx-auto">
  <%= admin_breadcrumbs(@breadcrumbs) %>

  <%= render layout: "components/ui/page_header",
             locals: { title: "Edit user", description: @user.username } do %><% end %>

  <%= render "form" %>
</div>
