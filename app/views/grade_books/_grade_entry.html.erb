<tr>
  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
    <%= entry.user.username.presence || content_tag(:span, "No username set", class: "text-gray-400 italic") %>
  </td>

  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
    <%= select_tag "grade_entry[#{entry.id}][math_grade]",
      options_for_select(GradeEntry::GRADE_OPTIONS, entry.math_grade),
      include_blank: true,
      class: "block w-full rounded-md border-gray-300 shadow-xs focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
  </td>

  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
    <%= select_tag "grade_entry[#{entry.id}][reading_grade]",
      options_for_select(GradeEntry::GRADE_OPTIONS, entry.reading_grade),
      include_blank: true,
      class: "block w-full rounded-md border-gray-300 shadow-xs focus:border-indigo-500 focus:ring-indigo-500 sm:text-sm" %>
  </td>

  <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
    <%= entry.attendance_days.present? ? entry.attendance_days : content_tag(:span, "—", class: "text-gray-400") %>
  </td>
</tr>
