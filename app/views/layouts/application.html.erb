<!DOCTYPE html>
<html>
  <head>
    <title>StocksInTheFuture</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  </head>
  <body class="h-screen w-screen overflow-hidden">
    <% if user_signed_in? %>
      <div class="fixed top-0 left-0 w-full z-50 flex items-center justify-between py-4 px-6 h-16 bg-[#f7f9f3]">
        <%= image_tag "SITF-Horz-no-tag-logo.svg", class: "h-8 w-auto", alt: "Stocks in The Future" %>
        <%= render 'layouts/stock_ticker' %>
      </div>

      <div class="fixed w-full h-px z-40 top-16 bg-[#004f6b]"></div>

      <div class="fixed z-40 flex w-40 bottom-0">
        <div class="flex grow flex-col gap-y-5 overflow-y-auto px-0 pt-4 pb-4 bg-[#00698c]">
          <%= render 'layouts/navbar' %>
        </div>
      </div>

      <main class="h-full overflow-auto p-6 ml-40 mt-[66px] bg-[#f7f9f3]">
        <%= yield %>
      </main>
    <% else %>
      <main class="h-full overflow-auto p-6 bg-[#f7f9f3]">
        <%= yield %>
      </main>
    <% end %>
  </body>
</html>
