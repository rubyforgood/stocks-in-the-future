<div class="rounded-xl border border-black/60 overflow-hidden bg-white">
  <div class="bg-amber-300 py-3 px-5 font-medium text-sm tracking-wide rounded-t-xl">Investment Earnings Summary</div>
  <div class="divide-y divide-black/40 text-sm">
    <% summary_rows = [
         ['Attendance Earnings', @earnings_summary.attendance_earnings_cents / 100.0],
         ['Reading Earnings', @earnings_summary.reading_earnings_cents / 100.0],
         ['Math Earnings', @earnings_summary.math_earnings_cents / 100.0],
         ['Rewards', @earnings_summary.awards_cents / 100.0]
       ] %>
    <% summary_rows.each do |label, amount| %>
      <div class="flex items-center justify-between py-4 px-5 bg-white">
        <span><%= label %></span>
        <span class="tabular-nums"><%= number_to_currency(amount) %></span>
      </div>
    <% end %>
    <div class="flex items-center justify-between py-4 px-5 font-semibold bg-[#eceec8]">
      <span>Total Earnings</span>
      <span class="tabular-nums"><%= number_to_currency(@earnings_summary.total_earnings_cents / 100.0) %></span>
    </div>
    <div class="flex items-center justify-between py-4 px-5 font-medium text-red-600">
      <span>Transaction Fees</span>
      <span class="tabular-nums"><%= number_to_currency(@earnings_summary.transaction_fees_cents / 100.0) %></span>
    </div>
  </div>
</div>
